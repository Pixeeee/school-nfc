
import { useState } from "react";import { School,ShieldCheck } from "lucide-react";import { SchoolNfc } from "../native/plugin";import { Banner,Button,Field } from "../components/Ui";
export function LoginScreen({onLogin}:{onLogin:()=>void}){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");if(!/^\S+@\S+\.\S+$/.test(email)){setError("Enter a valid email address.");return}if(password.length<8){setError("Password must contain at least 8 characters.");return}try{setBusy(true);await SchoolNfc.signIn({email:email.trim().toLowerCase(),password});onLogin()}catch(e){setError(e instanceof Error?e.message:"Sign-in failed.")}finally{setBusy(false)}}return <main className="auth-screen"><div className="mobile-brand"><div><School/></div><span>School NFC</span></div><section className="auth-card"><span className="eyebrow">TEACHER DEVICE</span><h1>Ready for a faster school entrance.</h1><p>Sign in with your authorized school account. This phone must also be approved before it can scan.</p>{error&&<Banner>{error}</Banner>}<form onSubmit={submit}><Field label="Email"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></Field><Field label="Password"><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></Field><Button type="submit" busy={busy}>Sign in securely</Button></form><div className="security-note"><ShieldCheck/><span>No student or parent information is written to NFC cards.</span></div></section></main>}
